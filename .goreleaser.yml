version: 2

# This repository (aws-tui) hosts multiple AWS TUI tools. Each tool has its
# own build/archive/cask entry keyed by id; all tools are released together
# under a single tag.
project_name: aws-tui
env:
  - GO111MODULE=on
before:
  hooks:
    - go mod tidy
builds:
  - id: aws-parameter-store-tui
    main: ./cmd/aws-parameter-store-tui
    binary: aws-parameter-store-tui
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
      - -X main.Revision={{.ShortCommit}}
    env:
      - CGO_ENABLED=0
    ignore:
      - goos: windows
        goarch: "386"
      - goos: darwin
        goarch: "386"
      - goos: linux
        goarch: "386"
      - goos: linux
        goarch: arm
        goarm: "7"
archives:
  - id: aws-parameter-store-tui
    ids:
      - aws-parameter-store-tui
    name_template: >-
      aws-parameter-store-tui_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - README.md
      - LICENSE
      - CREDITS
release:
  prerelease: auto
checksum:
  name_template: 'checksums.txt'
snapshot:
  version_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

homebrew_casks:
  - name: aws-parameter-store-tui
    ids:
      - aws-parameter-store-tui
    directory: Casks
    repository:
      owner: takaishi
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: goreleaser@carlosbecker.com
    description: "TUI for browsing AWS Systems Manager Parameter Store"
    homepage: "https://github.com/takaishi/aws-tui"
